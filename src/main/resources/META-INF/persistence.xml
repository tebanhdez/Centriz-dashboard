<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
             http://xmlns.jcp.org/xml/ns/persistence/persistence_2_1.xsd"
             version="2.1">
  <persistence-unit name="centrizManager" transaction-type="RESOURCE_LOCAL">
    <provider>org.hibernate.ejb.HibernatePersistence</provider>
    <class>Customer</class>
    <properties>
     	<property name="hibernate.connection.driver_class" value="org.postgresql.Driver" />
        <property name="hibernate.connection.password" value="centriz" />
        <property name="hibernate.connection.url" value="jdbc:postgresql://localhost:5432/centriz" />
        <property name="hibernate.connection.username" value="centriz" />
        <property name="hibernate.dialect" value="org.hibernate.dialect.PostgreSQLDialect" />
        <!-- Possible values: -->
        <!-- 'create'       -> creates the schema, destroying previous data
             'create-drop'  -> drop the schema at the end of the session 
             'update'       -> update the schema, 
             'validate'     -> validates the schema, no changes on DB -->
        <property name="hibernate.hbm2ddl.auto" value="create"/>
        <property name="hibernate.hbm2ddl.import_files" value="initializeDB.sql" />
    </properties>
  </persistence-unit>
</persistence>